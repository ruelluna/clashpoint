---
description: React Server vs Client Component patterns
globs: "**/*.{tsx,jsx}"
alwaysApply: false
---

# Components

## Server vs Client

Default to Server Components. Add `'use client'` only when the component needs:

- Event handlers (`onClick`, `onChange`)
- React hooks (`useState`, `useEffect`, `useRef`)
- Browser APIs (`window`, `localStorage`)

Push the client boundary as far down the tree as possible.

```tsx
// GOOD — small client island inside server page
export default async function EventPage({ params }: Props) {
  const event = await getEvent(params.id)
  return (
    <div>
      <EventHeader event={event} />
      <JoinFightButton eventId={event.id} />
    </div>
  )
}
```

## Forms

- Prefer `<form action={serverAction}>` over manual fetch calls
- Use `useActionState` for action feedback (pending, errors, success)
- Use `useFormStatus` in a child submit button for loading state
- For instant feedback on high-confidence mutations (join, toggle), see `optimistic-ui.mdc`

## Composition

- Keep components under ~150 lines; extract subcomponents when growing
- Colocate feature-specific components in `features/{module}/components/`
- Shared layout primitives live in `components/dashboard/`; color mode and hybrids in `components/ui/`

## Styling

- **Chakra UI v3** is the primary component layer for app and dashboard UI
- Use `components/dashboard/` primitives (`PageStack`, `PanelCard`, `DetailFieldRow`, `ButtonGroup`) before bespoke layout
- Every client component that touches layout must satisfy `responsive-ui.mdc` (mobile-first breakpoints, touch targets)
- Tailwind is for global tokens and root layout classes only — not responsive dashboard/feature UI
- Use `cn()` from `lib/utils` only where Tailwind classes are already in use (e.g. rich text)
